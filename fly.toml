app = "jihenb-job-board"
primary_region = "cdg"

[build]

[env]
  DATA_DIR = "/data"
  PORT = "8765"

[http_service]
  internal_port = 8765
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"

[[mounts]]
  source = "job_board_data"
  destination = "/data"
